// Single source of truth for the channel's video format.
// Individual scenes can override these in the registry,
// but prefer changing them here so every scene stays consistent.

export const FPS = 30;
export const WIDTH = 1920;
export const HEIGHT = 1080;
